[pytest]
testpaths = tests
markers =
    unit: Unit tests – fast, no external dependencies (deselect with '-m "not unit"')
    integration: Integration tests – may call external services
    e2e: End-to-end tests – full system tests
